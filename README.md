# MotionCue — marketing landing

Static landing page for MotionCue, the watch-first stretch app for desk workers.
Served via GitHub Pages. Pre-launch: `index.html` carries a `noindex` meta tag —
remove it at official launch to allow search indexing.
